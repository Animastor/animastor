<?xml version="1.0" encoding="utf-8"?>
<!-- Web-parity notice panel for simple confirmations (.modal__notice):
     colorSurfaceVariant fill + thin dashed colorOutline stroke + 12dp corners,
     using Android tokens (web: surface-2 / 1px dashed outline / radius-sm). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurfaceVariant" />
    <stroke
        android:width="1dp"
        android:color="?attr/colorOutline"
        android:dashWidth="4dp"
        android:dashGap="3dp" />
    <corners android:radius="12dp" />
</shape>
