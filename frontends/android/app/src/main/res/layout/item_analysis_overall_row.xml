<?xml version="1.0" encoding="utf-8"?>
<!-- Overall row of the Parallel AI Analysis panel (web OverallRow parity):
     aggregate (completed + failed) / total percent, phase timer and the
     "Total analysis time: mm:ss" caption once all tasks are terminal. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/analysisOverallRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="3dp"
    android:paddingBottom="3dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/analysisOverallLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="12sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/analysisOverallPercent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:minWidth="30dp"
            android:gravity="end"
            android:paddingStart="6dp" />

        <TextView
            android:id="@+id/analysisOverallTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:singleLine="true"
            android:gravity="end"
            android:paddingStart="8dp"
            android:text="00:00:00" />
    </LinearLayout>

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/analysisOverallBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        app:trackThickness="4dp"
        app:trackCornerRadius="2dp"
        app:indicatorColor="?attr/colorSecondary"
        app:trackColor="?attr/colorSurfaceVariant" />

    <TextView
        android:id="@+id/analysisOverallTotal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:singleLine="true"
        android:ellipsize="end"
        android:layout_marginTop="2dp"
        android:visibility="gone" />
</LinearLayout>
