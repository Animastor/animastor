// HOST SHIM — re-export of the @animastor/parser package.
// Legacy path kept for consumers (relocation checklist §2.4).
// The structureDetector port binding API lives here.

module.exports = require('@animastor/parser');
