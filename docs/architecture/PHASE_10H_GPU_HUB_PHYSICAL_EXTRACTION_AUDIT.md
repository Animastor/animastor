# PHASE 10H — GPU Hub Physical Extraction Audit

**Status: PASS**
**Date:** 2026-09-06
**Source commit (baseline HEAD):** `1916e80799914534ac7b4739953b8e702ace1c90`
**Target repository:** `Animastor/animastor-gpu-hub` — independent repo, package `@animastor/gpu-hub@0.1.0`
**Predecessors:** 10A (contract freeze) → 10B (canonical protocol import) → 10C/10D (extraction readiness / package boundary) → 10E (release readiness) → 10F (contracts publish) → 10G (registry dependency) → **10H (physical extraction)**
**Principle:** *Cut GPU Hub out of the monorepo as a self-contained package — byte-identical runtime, zero monorepo imports, contracts stays a registry dependency, artifact mounts fixed as an external deployment contract.*

---

## 1. Extraction method & Git history

`git filter-repo --subdirectory-filter gpu-hub` against a fresh clone of
`Animastor/animastor@1916e807`:

- **38 commits preserved** — exactly the commits that touched `gpu-hub/`
  (authors, dates, full messages intact; hashes rewritten by the filter).
  History spans `380a7773` (Recovery01, hub sources imported) →
  `1916e807` (Phase 10G registry migration).
- Monorepo trees (backend/worker/frontend/contracts/docs) were NOT carried over;
  filter-repo pruned all non-hub paths and empty commits.
- New repo HEAD: one Phase 10H commit on top of the rewritten history
  (metadata + docs only — see §2).

## 2. Extracted files

| File | Status vs monorepo `gpu-hub/` |
|---|---|
| `gpu-hub.js` (2036 lines) | **byte-identical** (sha256) |
| `server.js` | **byte-identical** |
| `tarball.js` | **byte-identical** |
| `bootstrap.js` | **byte-identical** |
| `tests/run-all.cjs` (19 checks) | **byte-identical** |
| `Dockerfile`, `.dockerignore` | **byte-identical** |
| `README.md`, `LICENSE` (MIT) | **byte-identical** |
| `package-lock.json` | **byte-identical** (registry resolution `@animastor/contracts@0.1.0` from npmjs) |
| `package.json` | metadata-only diff: `repository.url`/`bugs.url` → `Animastor/animastor-gpu-hub` (standalone identity; no runtime field touched — name/version/main/license/engines/files/scripts/dependencies unchanged) |
| `EXTRACTION.md`, `DEPLOYMENT.md`, `.gitignore` | added (docs/housekeeping, excluded from the Docker image by `.dockerignore`) |

## 3. Omitted files — and why

| Omitted | Why |
|---|---|
| `node_modules/` | regenerated by `npm ci` |
| `backend/`, `worker/`, `frontends/`, `contracts/` sources | monorepo trees — explicitly excluded from scope |
| `backend/tests/gpu-hub-*.test.js`, `tests/architecture/*` | monorepo-side behavioral suites; they keep running in the monorepo against the same frozen sources. The standalone `tests/run-all.cjs` adds package-boundary checks (dependency isolation, contracts resolution, route freeze, Redis ownership) without replacing them |
| `docs/architecture/*` (GPU_HUB_CONTRACT.md, JOB_PROTOCOL_V2.md, audits) | monorepo-owned normative records; README references remain valid |
| Job Protocol implementation | **never copied** — consumed from `@animastor/contracts ^0.1.0` (npm registry) |

## 4. Dependency boundary

- Frozen runtime dependency set (unchanged, guard-pinned):
  `@animastor/contracts ^0.1.0`, `cors ^2.8.5`, `express ^4.19.2`,
  `ioredis ^5.10.0` + Node builtins (`crypto`, `fs`, `path`, `zlib`, `http`, `https`, `url`).
- **No** `file:../contracts`, **no** root fallback, **no** local Job Protocol copy:
  `npm pack --dry-run` = 9 files / 117.8 kB; `require('@animastor/contracts')`
  resolves from `node_modules` (registry install verified inside the container).
- Protocol parity: hub `PROTOCOL_VERSION` === contracts `PROTOCOL_VERSION` === `2`.
- Zero monorepo/root imports (regex guard in the suite + manual grep):
  no `require('../…')`, no `backend/…`, `worker/…`, `frontends/…` specifiers.

## 5. Deployment seams (frozen, unchanged)

| Seam | Contract |
|---|---|
| Service identity | Docker service `gpu-hub`, node:20, `CMD node server.js`, port `5000`, internal DNS `http://gpu-hub:5000` |
| nginx | `/gpu/` prefix → hub `:5000` (public path = hub path under `/gpu/`) |
| Auth | dual plane: backend `x-api-key` (`/task`, `/queue/clear`, fail-closed 503 `hub_api_key_not_configured` when unset); worker `Bearer wrk.<id>.<secret>` (mirror hget `animastor:worker-auth` by sha256(secret), fail-closed 401) |
| Redis | single `REDIS_URL`; hub-owned families (`animastor:gpu-hub:workers`, queue/running/processing/claimed/dead-letter/heartbeat/result/error/job-dedup); hub READS (never writes) `animastor:worker-auth` |
| Backend callbacks | `POST ${BACKEND_URL}/gpu/task/result` / `/gpu/task/error`, Job Protocol v2 envelope, 5 retries × 500 ms, error fallback key TTL 1 h |
| Timeout/retry/cancel | `GPU_TIMEOUT_MS` per-job/per-GPU timeout via 10 s sweep; orphan grace/requeue → dead-letter; `DELETE /queue/clear` scopes; dedup DEL on error/timeout paths — all untouched |
| Env | `PORT`, `REDIS_URL`, `BACKEND_URL`, `GPU_HUB_API_KEY`, `GPU_HUB_ALLOW_OPEN` (dev-only), `GPU_TIMEOUT_MS`/`GPU_TIMEOUT`, `SHARE_FEATURES_ENABLED`, `ORPHAN_GRACE_MS`, `MAX_ORPHAN_REQUEUES` |

## 6. Artifact mounts — external deployment contract (recorded, NOT re-architected)

The five read-only artifact mounts remain **outside** the new repo. They are fixed as
an external deployment contract (`DEPLOYMENT.md` in the target repo): the deployment
must mount, read-only, at the FROZEN container paths:

| Container path | Serves | Monorepo reference source |
|---|---|---|
| `/app/worker-source/worker.cjs` | `GET /worker-source` (deprecated) | `worker/worker/worker.cjs` |
| `/app/worker-bundle` | `GET /worker-bundle(+/sha256)` | `worker/worker/` |
| `/app/workflows` | `GET /workflow/:id` | `backend/ai/workflows/` |
| `/app/installer-src` | `GET /installer(/bundle,/sha256)` | `backend/src/installer/` |
| `/app/install-manifests` | allowlist for workflows/profiles | `backend/ai/install-manifests/` |

Missing mounts degrade to the frozen 404 tokens
(`worker_source_unavailable` / `worker_bundle_unavailable` /
`installer_unavailable` / `workflow_not_found`) — verified in the smoke test.
Architectural replacement of these mounts is explicitly out of scope for 10H.

## 7. Verification results (target repo)

| Check | Result |
|---|---|
| `npm ci` (clean tree, registry) | ✅ PASS |
| Standalone tests | ✅ **19/19 PASS** |
| `@animastor/contracts` resolution from npm | ✅ PASS (`node_modules/@animastor/contracts/src/index.js`, v0.1.0, no file: fallback) |
| `npm pack --dry-run` | ✅ PASS (9 files / 117.8 kB, `@animastor/gpu-hub@0.1.0`) |
| `require('@animastor/gpu-hub')` | ✅ PASS (factory + `PROTOCOL_VERSION === 2` parity with contracts) |
| Docker build | ✅ PASS |
| Docker smoke (isolated redis:7 network) | ✅ PASS — `/health`; `/task` no-key→401, bad-key→401, good-key→`{ok:true}`, duplicate→`{ok:true,duplicate:true}`; key unset→**503 `hub_api_key_not_configured`**; `/beacon`,`/task/next` unauth→401; `/queue/clear` bad-key→401, good-key→`removed:{queued,dedup}`; artifact routes without mounts→frozen 404 tokens; `/app` contains no monorepo files; `PROTOCOL_VERSION=2` inside container; exactly 14 routes |
| No monorepo/root imports; no protocol copies | ✅ PASS (suite + grep) |

## 8. Extraction integrity audit (old vs new)

- **Runtime sources:** sha256-identical, byte-for-byte (§2) — the running code is THE
  same code; no semantic drift is possible.
- **package-lock.json:** sha256-identical → identical dependency graph in the container.
- **Dockerfile/.dockerignore:** sha256-identical → identical image build semantics.
- **Behavioral smoke (§7)** exercised the frozen surface (auth planes, fail-closed
  posture, dedup, queue/clear, protocol gates, artifact 404s) and matches
  `GPU_HUB_CONTRACT.md` §3/§4/§8/§11 — the same behavior the monorepo hub exhibits.
- **Monorepo deployment untouched:** `docker-compose.yml`, backend/worker/proxy configs
  unchanged in this phase; the running production `gpu-hub` container (monorepo build)
  was not restarted or modified.

## 9. Known remaining coupling

1. **Five artifact mounts** depend on monorepo-owned trees (§6) — external deployment
   contract, replacement deferred.
2. **Worker-token grammar** — hub carries the hand-synced `parseWorkerToken` copy
   (frozen debt, parity-guarded; consolidation into contracts is a separate phase).
3. **`animastor:worker-auth` mirror** — hub reads the backend-owned key (frozen
   worker-auth debt, unchanged).
4. **Monorepo-side test suites** still point at `gpu-hub/` paths inside the monorepo —
   they are unaffected while the monorepo copy remains (next phase decides the
   cutover/deletion).
5. **GitHub push of the target repo** — local bare mirror
   `/home/animastor/repos/animastor-gpu-hub.git` is filled and carries the `github`
   remote (`git@github.com:Animastor/animastor-gpu-hub.git`); the repo must exist on
   GitHub (same provisioning path as `animastor.git`) — `git push github master` from
   the mirror completes publication.

## 10. Rollback plan

- Monorepo: **no functional change was made** — `gpu-hub/`, compose, backend, worker
  are untouched; rollback = revert the audit-doc commit (docs-only).
- Target repo: delete `/home/animastor/repos/animastor-gpu-hub.git` +
  `/home/animastor/animastor-gpu-hub` and (once created) the GitHub repo. The monorepo
  continues to build/deploy GPU Hub from `gpu-hub/` exactly as before.
- The extraction is additive: until the next phase switches `docker-compose.yml` to the
  external image, both trees coexist byte-identically.

## 11. Verdict

**PHASE 10H: PASS.** GPU Hub is physically extracted into
`Animastor/animastor-gpu-hub` as `@animastor/gpu-hub@0.1.0` with preserved Git
history, byte-identical runtime, registry-only contracts dependency, frozen
14-route/API/auth/Redis/callback semantics, documented external artifact-mount
contract, and a green verification matrix (npm/tests/pack/docker). npm publish is
NOT part of this phase. Monorepo deployment is untouched and remains canonical
until the next phase performs the cutover.
